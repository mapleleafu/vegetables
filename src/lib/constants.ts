export const GAME_CONFIG = {
  // Economy
  DEFAULT_WORD_COIN_VALUE: 1,
  DEFAULT_MAX_COINS_PER_WORD: 10,

  // Rewards
  POINTS_PER_PRACTICE: 1, // Points given when word is already maxed
} as const;
